# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Node / Vite
node_modules/
packages/frontend/dist/
packages/frontend/.vite/

# Rust / Tauri
packages/frontend/src-tauri/target/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Local env
.env
.env.local
*.local

# Logs (committed evidence stays under logs/, not gitignored. ad-hoc local logs only)
*.log

# Runtime audit-write side directory (not the committed logs/ evidence tree)
.apohara/

# --- Internal planning + release prep (mirrored to ~/Documentos/Apohara_PRIVATE/) ---
# Maintainer-only artifacts: video scripts, outreach drafts, submission
# text, checklists, paper-upload workflow. Public repo carries only the
# shippable surface (code, docs, audit, license, public-facing assets).
.omc/
docs/video-*.md
docs/outreach-*.md
docs/*-submission.md
docs/pre-submit-*.md
docs/paper-upload-guide.md
docs/paper-metadata.yaml
